{{!-- <header>
    <div class="nav-top">
        <div class="container">
            <div class="row justify-content-between align-items-center">
                <div class="col-3 logo-none">
                    <div class="logo">
                        {{#each navInfo}}
                        <img src="/images/{{img}}" alt="{{img}}" width="200" height="67">
                        <p class="navTitle">{{navtitle}}</p>
                        {{/each}}
                    </div>
                </div>
                <div class="col-5">
                    <div class="nav-top-navbar">
                        <ul class="nav">
                            <li class="nav-item">
                                <a class="nav-link active" aria-current="page" href="/">Bosh sahifa</a>
                            </li>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                                    data-bs-toggle="dropdown" aria-expanded="false">
                                    Ma'lumotlar
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                    {{#each category}}
                                    <li><a class="dropdown-item" href="/view/{{id}}">{{categoryName}}</a></li>
                                    {{/each}}
                                </ul>
                            </li>
                        </ul>
                    </div>
                </div>
                <div class="col-md-6 col-lg-3">
                    <form class="d-flex">
                        <input class="form-control me-2" type="search" placeholder="Qidiruv..." aria-label="Search">
                        <button class="btn btn-outline-primary" type="submit">Qidirish</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</header> --}}

{{#each category}}
<div class="section-html" style="background: {{background}} !important;">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="html-info">
                    <h1 class="html-head">{{categoryName}}</h1>
                    <p class="html-text">The language for building web pages</p>
                    <a href="/view/{{id}}" class="html-btn">Learn {{categoryName}}</a>
                </div>
            </div>
        </div>
    </div>
</div>
{{/each}}